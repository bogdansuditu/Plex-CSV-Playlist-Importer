services:
  web:
    build: .
    container_name: plex-csv-playlist-importer
    env_file:
      - .env
    environment:
      - APP_PORT=${APP_PORT:-8080}
    ports:
      - "${APP_PORT:-8080}:${APP_PORT:-8080}"
    restart: unless-stopped
